# PresenceIQ Avatar (Person 1) — copy to .env.local
# Index: ../docs/ENV.md
# Beyond Presence API key lives on backend only — see docs/BEYOND_PRESENCE.md

VITE_BACKEND_URL=http://localhost:3000
VITE_BP_WEBHOOK_SECRET=dev-bp-secret-change-me

# Optional — not used by SDK v1 (BP dashboard / backend pipeline)
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
