/**
 * Legacy reference — see `src/index.ts` for the implemented integration.
 * For SDK usage from demo sites (frozen frontend boot.ts):
 *
 *   import { initPresenceIQAvatar } from "@presenceiq/avatar";
 *
 * See ../index.ts and ../../EMBED.md
 */
export { bootstrap, onPresenceIQReady } from "../index";
