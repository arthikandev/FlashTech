# PresenceIQ Frontend (Person 3) — copy to .env.local
# Index: ../docs/ENV.md
# VITE_CONVEX_URL must match backend NEXT_PUBLIC_CONVEX_URL

# Optional. If unset, dev uses Vite proxy /app → http://localhost:3001 (friend's default).
# Production: set here, in Vercel, or edit public/runtime-config.json on main only.
VITE_BACKEND_URL=
VITE_CONVEX_URL=https://adamant-puffin-769.convex.cloud
