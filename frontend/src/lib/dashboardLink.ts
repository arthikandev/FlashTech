/** In-app routes for landing CTAs (setup-clerk uses Vite /dashboard). */
export function getDashboardEntryPath(): string {
  return "/login";
}

export function getDashboardHref(): string {
  return "/dashboard";
}
