/**
 * Local landing / embed preview clip. Served from `frontend/public/preview.mp4`.
 * Replace that file with your clip (same path as bundled asset for Vite/Vercel).
 */
export const PREVIEW_VIDEO_SRC = "/preview.mp4";

/** Beyond Presence live section (`preview (2).mp4`). */
export const BEYOND_PRESENCE_VIDEO_SRC = "/beyond-presence-preview.mp4";

/** Features bento video tile (`preview (1).mp4`). */
export const FEATURES_VIDEO_SRC = "/features-preview.mp4";
